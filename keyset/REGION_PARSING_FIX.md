# Исправление парсинга по регионам

## Что было исправлено

### 1. Пакетный сбор (Кнопка "📦 Пакет")

**Проблема:** При пакетном сборе фраз выбранные регионы не сохранялись и не применялись при последующем парсинге.

**Решение:**

#### Файл: `app/dialogs/batch_collect_dialog.py`

В методе `_on_start_collect()` добавлено:
- Извлечение выбранных регионов из `RegionSelector`
- Создание `regions_map` (словарь `{region_id: region_path}`)
- Формирование `region_names` (список названий регионов)
- Добавление настроек режимов парсинга (`modes`, `ws`, `qws`, `bws`)

Теперь настройки передаются в формате, совместимом с частоткой:

```python
settings = {
    "geo_ids": selected_ids,
    "regions": selected_ids,
    "regions_map": {id: path, ...},
    "region_names": ["Россия / Москва", ...],
    "collect_wordstat": True,
    "modes": ["ws"],
    "ws": True,
    ...
}
```

#### Файл: `app/tabs/parsing_tab.py`

В методе `_on_batch_collect_requested()`:
- Добавлена нормализация настроек через `_normalize_wordstat_settings()`
- Настройки сохраняются в `self._last_settings`
- При запуске парсинга кнопкой "🚀 Запустить парсинг" будут использованы эти сохраненные регионы

**Результат:** Теперь при пакетном сборе:
1. Выбираете регионы в диалоге "Пакетный сбор"
2. Добавляете фразы
3. Нажимаете "Начать сбор"
4. **СРАЗУ запускается парсинг** - открываются браузеры и начинается сбор данных по выбранным регионам!
5. Результаты автоматически добавляются в таблицу

---

### 2. Частотка (Кнопка "Частотка")

**Проверка:** Частотка уже правильно работает с регионами!

**Как работает:**
1. При открытии выпадающего меню "Частотка" виджет `WordstatDropdownWidget` загружает сохраненные настройки из `_last_settings`
2. `GeoSelector` отображает ранее выбранные регионы или Россию (225) по умолчанию
3. При выборе регионов они сохраняются в `regions_map`
4. При нажатии "Получить данные" настройки передаются в `_run_parsing_with_settings()`
5. `MultiParsingWorker` создает `region_plan` для каждого региона
6. `SingleParsingTask` вызывает `turbo_parser_10tabs()` с `region_id` для каждого региона
7. `TurboParser` использует `self.region_id` при формировании URL:
   ```python
   url = f"https://wordstat.yandex.ru/?words={phrase}&region={self.region_id}&lr={self.region_id}"
   ```

**Результат:** Частотка парсит по выбранным регионам, а не по всей РФ!

---

## Цепочка работы с регионами

### Для частотки:

```
WordstatDropdownWidget (выбор регионов)
    ↓
get_settings() → regions_map = {id: path}
    ↓
_on_dropdown_parsing_requested() → нормализация и сохранение в _last_settings
    ↓
_run_parsing_with_settings() → создание MultiParsingWorker
    ↓
MultiParsingWorker → region_plan = [(region_id, region_name), ...]
    ↓
SingleParsingTask.run() → для каждого региона:
    ↓
turbo_parser_10tabs(region_id=region_id)
    ↓
TurboParser.run() → использует self.region_id в URL и API запросах
```

### Для пакетного сбора:

```
BatchCollectDialog (выбор регионов)
    ↓
_on_start_collect() → формирование regions_map и region_names
    ↓
_on_batch_collect_requested() → нормализация и сохранение в _last_settings
    ↓
СРАЗУ вызывается _run_parsing_with_settings() → создание MultiParsingWorker
    ↓
[Далее как в частотке - парсинг по выбранным регионам...]
```

---

## Тестирование

### Сценарий 1: Пакетный сбор с выбором региона

1. Открыть приложение
2. Перейти на вкладку "Парсинг"
3. Убедиться, что есть хотя бы один аккаунт на вкладке "Аккаунты"
4. Нажать "📦 Пакет"
5. В левой панели снять "Все регионы"
6. Выбрать регион (например, "Омск")
7. Ввести фразы (например, "купить телефон")
8. Нажать "▶ Начать сбор"
9. Проверить в журнале:
   - "📦 ПАКЕТНЫЙ СБОР ФРАЗ"
   - "🌍 Регионы: Россия / Сибирь / Омск"
   - "🚀 Запуск парсинга..."
10. **Должны сразу открыться браузеры Chromium** с профилями из БД
11. Проверить в логах парсера: должен быть `region=66` в URL
12. Результаты автоматически добавляются в таблицу

### Сценарий 2: Частотка с выбором региона

1. Нажать "Частотка"
2. В выпадающем меню выбрать регион (например, "Санкт-Петербург")
3. Нажать "Получить данные"
4. Проверить в журнале: "🌍 Регионы: Россия / Санкт-Петербург"
5. Проверить в логах парсера: должен быть `region=2` в URL

### Сценарий 3: Проверка автоматического запуска парсинга

1. Нажать "📦 Пакет"
2. Выбрать несколько регионов (например, "Москва", "Санкт-Петербург")
3. Ввести 3-5 фраз
4. Нажать "▶ Начать сбор"
5. **Парсинг должен запуститься автоматически без дополнительных кликов**
6. Проверить в журнале: для каждого региона должна быть запись "🌍 Регион: ..."
7. После завершения парсинга - все результаты в таблице

---

## Изменённые файлы

1. `app/dialogs/batch_collect_dialog.py` - метод `_on_start_collect()`
2. `app/tabs/parsing_tab.py` - метод `_on_batch_collect_requested()`

---

## Совместимость

Изменения полностью совместимы с существующим кодом:
- `_normalize_wordstat_settings()` уже умеет обрабатывать все форматы
- `MultiParsingWorker` уже умеет работать с `regions_map`
- `turbo_parser_10tabs()` уже принимает `region_id`

---

**Дата:** 2025-10-31
**Автор:** Claude Code
