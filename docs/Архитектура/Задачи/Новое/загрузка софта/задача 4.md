Анализ правильный. Задача понятна. 

ЦЕЛЬ: консолидировать рабочую структуру так, чтобы вся бизнес-логика находилась в KeySet-MVP (не разбросана по подпапкам), и при этом не сломать текущую работающую связку launcher → backend → keyset services.

ЗАДАЧИ:

1️⃣ ПРОВЕРЬ текущую цепочку импортов:
- Найди все места, где backend импортирует модули из keyset (например, backend/routers/accounts.py → keyset.services.accounts)
- Проверь, откуда launcher.py импортирует backend.main
- Убедись, что все работает при запуске python launcher.py

2️⃣ ПЕРЕНЕСИ рабочие модули keyset в корень KeySet-MVP:
- Скопируй все используемые модули из KeySet-MVP/keyset/* в структуру внутри KeySet-MVP (например, KeySet-MVP/keyset_services/* или KeySet-MVP/core/*)
- Сохрани структуру: если было keyset/services/accounts.py, должно стать keyset_services/accounts.py (или аналогичная схема)
- НЕ удаляй пока старую папку keyset

3️⃣ ОБНОВИ импорты в backend:
- Замени все импорты типа `from keyset.services import accounts` на новый путь
- Замени импорты в launcher.py, если там есть ссылки на keyset
- Проверь, что все роутеры (accounts.py, data.py, wordstat.py) корректно находят новые модули

4️⃣ ОБНОВИ PyInstaller spec:
- В build/keyset.spec замени старый collect_keyset() или datas на новые пути
- Убедись, что regions_tree_full.json, keyset.db и другие данные собираются из нового расположения
- НЕ трогай frontend/dist — он уже подключен

5️⃣ ТЕСТИРУЙ на каждом шаге:
- После каждого переноса запускай python launcher.py
- Проверяй, что API отвечает (curl http://127.0.0.1:{PORT}/api/health)
- Проверяй, что UI загружается и может получить данные из /api/accounts, /api/data/phrases

6️⃣ ФИНАЛЬНАЯ СБОРКА:
- Запусти npm run build в frontend
- Запусти build.ps1
- Проверь, что dist/KeySet.exe стартует и работает полностью

ВАЖНО:
- НЕ удаляй папку KeySet-MVP/keyset до успешной финальной сборки и проверки
- НЕ меняй логику работы сервисов — только пути импорта
- Если что-то сломается, откатывай импорты и проверяй снова

Начинай с п.1 — покажи список всех импортов keyset в backend.